server:
  port: 8090

app:
  jwt:
    # CHANGE THIS in production: use openssl rand -hex 32
    secret: "GainsSystemsSecretKey2024PleaseChangeInProduction!!"
    expiration: 86400000  # 24 hours in milliseconds

spring:
  main:
    banner-mode: "off"

logging:
  level:
    com.gains: INFO
    org.springframework.security: WARN
